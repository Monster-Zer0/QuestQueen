org.gradle.jvmargs=-Xmx1G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

neogradle.subsystems.parchment.minecraftVersion=1.21.1
neogradle.subsystems.parchment.mappingsVersion=2024.11.17

minecraft_version=1.21.1
minecraft_version_range=[1.21.1]
neo_version=21.1.249
loader_version_range=[1,)

mod_id=questqueen
mod_name=Quest Queen
mod_license=MIT
# Author/publish identity. Single-sourced here and expanded into neoforge.mods.toml, so a name change is a
# one-line edit rather than a rebuild-and-redeploy. QuestQueen = "Monster Zero" (provisional).
mod_authors=Monster Zero
# Issue tracker shown in the mod list. Left empty on purpose: no public tracker exists for this project yet,
# and a release must not advertise a URL that 404s. Set it here and ProcessResources will emit the field;
# while it is empty the line is dropped from the built mods.toml entirely.
mod_issue_tracker=
mod_version=1.1.202
mod_group_id=dev.aof.questqueen
sqlite_version=3.47.1.0
